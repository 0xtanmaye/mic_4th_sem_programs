DATA SEGMENT
NUM1 DB 09H
NUM2 DB 03H
RESULT DB ?
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START:
        MOV AX, DATA
        MOV DS, AX

        MOV AL, NUM1
        MOV BL, NUM2
        SUB AL, BL
        MOV RESULT, AL

        MOV AX, 4CH
        INT 21H
CODE ENDS
END START
