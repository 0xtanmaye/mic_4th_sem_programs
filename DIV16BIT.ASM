DATA SEGMENT
NUM1 DW 08H
NUM2 DW 02H
RESULT DW ?
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START:
        MOV AX, DATA
        MOV DS, AX

        MOV AX, NUM1
        MOV CX, NUM2
        DIV CX
        MOV RESULT, AX

        MOV AX, 4CH
        INT 21H
CODE ENDS
END START
