DATA SEGMENT
NUMS DW 1356H, 4522H, 3312H, 2244H, 2323H
LARGEST DW 00H
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START:
        MOV AX, DATA
        MOV DS, AX

        MOV SI, OFFSET NUMS
        MOV CX, 04H  ;IF CX=05 THEN DEC CX
        MOV AX, [SI]
        ;DEC CX
        ITER:
        INC SI
        INC SI
        CMP AX, [SI]
        JNB SKIP
        MOV AX, [SI]
        SKIP:
        DEC CX      ; LOOP ITER CAN BE USED INSTEAD OF THESE TWO
        JNE ITER    ; LINES TO AUTOMATICALLY DECREMENT AND JUMP TO ITER
        ;LOOP ITER

        MOV LARGEST, AX

        MOV AH, 4CH
        INT 21H
CODE ENDS
END START

